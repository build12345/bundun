apiVersion: v1
kind: Service
metadata:
  name: carry
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "carry"
    port: 8080
  selector:
    app: carry
