apiVersion: v1
kind: Service
metadata:
  name: volue
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "volue"
    port: 8080
  selector:
    app: volue
